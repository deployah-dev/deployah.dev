# Shell tape fixture

Two components so `deployah shell app` opens the huh picker. Own
project (`app`) so it does not overwrite the nginx hero.

`deployah cluster up` may rewrite `deployah.platform.yaml`.
