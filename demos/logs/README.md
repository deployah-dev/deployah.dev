# Logs tape fixture

Two workers print their hostname so `deployah logs app` shows two
prefixes. Own project (`app`) so it does not overwrite the nginx hero.

`deployah cluster up` may rewrite `deployah.platform.yaml`.
